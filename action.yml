name: 'Semantic Version Diff'
description: 'Provides semantic version information about a tag'
author: 'Nick Cipollo'
inputs:
  tag:
    description: 'An optional tag to be used for comparison. If this is omitted the git ref will be used (if it is a tag).'
    default: ''
runs:
  using: 'node12'
  main: 'lib/Main.js'
branding:
  icon: 'tag'
  color: 'gray-dark'

